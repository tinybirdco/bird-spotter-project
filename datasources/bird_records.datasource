
SCHEMA >
    `id` String,
    `name` String,
    `scientific_name` String,
    `family` String,
    `timestamp` DateTime,
    `country` String,
    `habitat` String,
    `genre` String

ENGINE "MergeTree"
ENGINE_PARTITION_KEY "toYYYYMM(timestamp)"
ENGINE_SORTING_KEY "country, name, timestamp"
